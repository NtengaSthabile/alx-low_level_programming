#include <stdio.h>
#include <stlib.h>
#include <time.h>

/**
 * main - Print the alphabet in lowercase and uppercase
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	char low;
	for (low = 'a'; low <= 'z' ; low * *)
		putchar(low);

	for (low = 'A'; low <= 'z' ; low * *)
		putchar(low);
		putchar('\n');

		return (0);
