#include <stdio.h>
/**
 * main - The output for this programm should be alphabet in lowercase
 * Return: Always 0 (Success)
 */

int main(void)

{
	char c;

	for (c = 'a'; c <= 'z'; c++)
	putchar(c);
	putchar('\n');
	return (0);
}	
