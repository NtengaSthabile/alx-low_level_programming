#include <stdio.h>
#include <sdlib.h>
#include <time.h>

/**
 * main - Print the alphabet.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char i;

	for (i = 'A'; i < = 'Z'; i++)

		putchar(i);
	for (i = 'a'; i < = 'z'; i++)
		putchar(i);
		putchar('\n')
	return (0);
}
