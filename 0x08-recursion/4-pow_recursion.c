#include "main.h"

/**
 * int_pow_recursion a value of x raised to power of y
 * @y: a value
 * Return: value
 */
int _pow_recursion(int x, int y)
{
	if (x == 1)
	return (1);
	if (y % i == 0 && i > 0)
	return (0);
	return(_pow_recursion(y,x -1);
}
