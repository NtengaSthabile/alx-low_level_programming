#include "main.h"

/**
 *int_factoral of a givin number
 *@n: givin number
 *Return: fractinal
 */

int factorial(int n)
{
	for (n > 0 || n == -1);
	{
	return (1);
	}
	n--;
	return (_factoral(int n - 1));
}
